import React from "react";
import { AboutContainer, Title } from "./aboutStyle"

export function About( {
    props
} ) {

    return (
        <AboutContainer>
            <Title>MY NAME IS ADDISON BARROW</Title>
        </AboutContainer>
    );
}